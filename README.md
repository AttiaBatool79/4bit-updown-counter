# 4bit-updown-counter
This project implements a 4-bit up/down counter using Verilog that operates at 1 Hz and displays the count on 4 LEDs and a 7-segment display. A switch controls the counting direction (up or down).
